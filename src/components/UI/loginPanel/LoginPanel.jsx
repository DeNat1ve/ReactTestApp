import React from 'react';
import './loginPanel.css'

const LoginPanel = () => {
    return (
        <div className="login">
            Login Panel Example
        </div>
    );
};

export default LoginPanel;